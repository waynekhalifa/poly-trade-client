export enum ShopContextSort {
	LOWEST_PRICE = 'lowest_price',
	HIGHEST_PRICE = 'highest_price',
	LOWEST_RATE = 'lowset_rate',
	HIGHEST_RATE = 'highest_rate'
}

export enum ShopContextLayout {
	GRID = 'grid',
	COLUMN = 'column'
}
